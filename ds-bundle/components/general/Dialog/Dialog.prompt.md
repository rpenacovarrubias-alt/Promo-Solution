Dialog from promo-solution. Use via `window.PromoSolution.Dialog` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BrowserRouter>` (full provider chain in README.md — components read theme/i18n from that context).

## Related

`DialogClose`, `DialogContent`, `DialogDescription`, `DialogFooter`, `DialogHeader`, `DialogOverlay`, `DialogPortal`, `DialogTitle`, `DialogTrigger`
