Tooltip from promo-solution. Use via `window.PromoSolution.Tooltip` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BrowserRouter>` (full provider chain in README.md — components read theme/i18n from that context).

## Related

`TooltipContent`, `TooltipProvider`, `TooltipTrigger`
