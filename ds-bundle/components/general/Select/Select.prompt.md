Select from promo-solution. Use via `window.PromoSolution.Select` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BrowserRouter>` (full provider chain in README.md — components read theme/i18n from that context).

## Related

`SelectContent`, `SelectGroup`, `SelectItem`, `SelectLabel`, `SelectScrollDownButton`, `SelectScrollUpButton`, `SelectSeparator`, `SelectTrigger`, `SelectValue`
